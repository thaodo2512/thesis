# Copy this file to `.env` and adjust values for your Jetson + camera
# Compose automatically loads `.env` from the repo root.

# HTTP port for the MJPEG camera stream
STREAM_PORT=8080

# CSI camera selection
# - On Jetson Nano B01 with two camera ports:
#   * 0 => CAM0 (near the barrel jack)
#   * 1 => CAM1 (near the HDMI)
CSI_SENSOR_ID=0

# CSI sensor mode (optional). Leave empty to let Argus pick a default.
# Common IMX219 modes (Jetson Nano):
#   0: 3264x2464 @ ~21 fps (4:3 full)
#   1: 3264x1848 @ ~28 fps (16:9)
#   2: 1920x1080 @ 30 fps  (16:9)
#   3: 1640x1232 @ 30 fps  (4:3)
#   4: 1280x720  @ 60 fps  (16:9)
# Set to 3 for a solid 1640x1232@30 preview, or 2 for 1080p@30.
CSI_SENSOR_MODE=3

# Output resolution and frame rate for the stream
CSI_WIDTH=1640
CSI_HEIGHT=1232
CSI_FPS=30

# Flip (0..7) applied by nvvidconv; 0 = none, 2 = H, 4 = V
CSI_FLIP=0

# --- Presets (IMX219) ---
# Uncomment one block below to quickly set a matching mode + resolution + fps.

# Preset: IMX219 1640x1232 @ 30 fps (recommended wide FOV preview)
# CSI_SENSOR_MODE=3
# CSI_WIDTH=1640
# CSI_HEIGHT=1232
# CSI_FPS=30

# Preset: IMX219 1920x1080 @ 30 fps (1080p video)
# CSI_SENSOR_MODE=2
# CSI_WIDTH=1920
# CSI_HEIGHT=1080
# CSI_FPS=30

# Preset: IMX219 1280x720 @ 60 fps (high frame rate)
# CSI_SENSOR_MODE=4
# CSI_WIDTH=1280
# CSI_HEIGHT=720
# CSI_FPS=60

